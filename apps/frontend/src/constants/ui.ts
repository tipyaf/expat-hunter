/** UI timing constants — extracted from components to avoid magic numbers */

/** Toast auto-dismiss timeout (ms) */
export const TOAST_DISMISS_MS = 8_000

/** Search progress polling interval (ms) */
export const SEARCH_POLL_INTERVAL_MS = 2_000

/** Profile save success feedback timeout (ms) */
export const SAVE_FEEDBACK_MS = 3_000

/** Chat typing animation delays (ms) */
export const CHAT_DOT_DELAY_1_MS = 0
export const CHAT_DOT_DELAY_2_MS = 150
export const CHAT_DOT_DELAY_3_MS = 300
